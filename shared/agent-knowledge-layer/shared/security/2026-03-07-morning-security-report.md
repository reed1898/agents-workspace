# Morning Security Report - 2026-03-07 07:00 (Asia/Shanghai)

## Scope
1. System security scan (exposure, services, ports, permissions)
2. OpenClaw risk scan (version, audit alerts, config/auth/exposure)
3. Skill safety scan (source trust, suspicious scripts, dangerous commands, privilege boundaries)

## Executive Summary
- High: 9
- Medium: 3
- Low: 2 (1 auto-fixed)
- Overall risk: **High** (third-party skills with critical code-safety hits + weak sandbox policy in multi-channel setup)

## Findings by Severity

### High
1. **Critical risky skills detected outside workspace** (`openclaw security audit --deep`)
   - Path family: `/Users/rain/.agents/skills/baoyu-*`
   - 8 critical findings across multiple skills, including:
     - `dangerous-exec` (child_process shell execution)
     - `env-harvesting` (env var access + network send patterns)
   - Affected skills include:
     - `baoyu-compress-image`
     - `baoyu-danger-gemini-web`
     - `baoyu-danger-x-to-markdown`
     - `baoyu-format-markdown`
     - `baoyu-image-gen`
     - `baoyu-post-to-wechat`
     - `baoyu-post-to-x`
     - `baoyu-url-to-markdown`
   - Impact: Potential command execution and credential/data exfiltration if invoked.

2. **OpenClaw default sandbox is off**
   - Config snapshot shows: `agents.defaults.sandbox.mode = "off"`
   - With runtime tools available, this increases impact of prompt/tool abuse.

3. **Secrets stored in plaintext config**
   - `~/.openclaw/openclaw.json` contains Telegram/Discord tokens, gateway token, talk API key, and skill credentials.
   - Impact: local compromise exposes all integration secrets immediately.

4. **A dev server appears bound to all interfaces**
   - Process found: `uvicorn ... --host 0.0.0.0 --port 8000`
   - Impact: LAN-reachable service if host firewall rules are permissive.

### Medium
1. **Potential multi-user exposure warning from OpenClaw**
   - `openclaw security audit` warns trust-model mismatch in shared/group contexts.
   - Discord allowlist channels/users are enabled while sandbox is off.

2. **Discord health probe warning**
   - `openclaw status --deep`: Discord `WARN` (`operation aborted`).
   - Not directly a vuln, but reduces confidence in channel observability and control-path reliability.

3. **Workspace skill warning: `reddit-research`**
   - `potential-exfiltration` heuristic in `scripts/reddit.ts:10` (file read + network send pattern).
   - Could be benign behavior; requires code review before wider trust.

### Low
1. **SSH client config permission too open (fixed)**
   - Before: `~/.ssh/config` mode `644`
   - After auto-fix: mode `600`
   - Action: `chmod 600 ~/.ssh/config`

2. **World-writable directories discovered (mostly expected/shared paths)**
   - Includes `~/Public/Drop Box` and many Parallels app-mapped directories.
   - Low immediate risk if these are expected virtualization/shared app locations.

## Auto-Fixes Applied
- [x] `~/.ssh/config` permissions hardened from `644` to `600`.

## OpenClaw Version / Audit Snapshot
- Version: `2026.3.2` (latest reported by local status)
- `openclaw security audit --deep`: **8 critical / 2 warn / 1 info**
- Attack surface summary: open groups `0`, allowlist groups `2`, browser control enabled, runtime tools enabled.

## Recommended Actions
1. **Immediate (today)**
   - Quarantine or remove untrusted `baoyu-*` skills under `~/.agents/skills/`.
   - Rotate tokens currently stored in `~/.openclaw/openclaw.json` (Telegram, Discord, talk API, gateway token, skill tokens).
2. **Hardening (today/tomorrow)**
   - Set `agents.defaults.sandbox.mode` to `all` (or at least stricter than `off`).
   - Restrict high-risk tools (runtime/fs/web) in any shared/group reachable contexts.
3. **Service exposure**
   - Verify whether `uvicorn --host 0.0.0.0:8000` is needed; if not, bind to `127.0.0.1`.
4. **Skill governance**
   - Enforce source allowlist + mandatory code-safety scan before enabling third-party skills.

## Evidence Commands
- `openclaw status`
- `openclaw status --deep`
- `openclaw security audit --deep`
- `ps aux | rg -i 'openclaw|gateway|node|python|sshd|nginx|docker'`
- `ls -ld ~/.ssh ~/.ssh/*`
- `find /Users/rain -maxdepth 3 -type d -perm -0002`

---
Generated by: 小洪 (daily 07:00 security cron)
