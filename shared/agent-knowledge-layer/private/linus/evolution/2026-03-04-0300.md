# Linus Evolver Cycle Log — 2026-03-04 03:00 (Asia/Shanghai)

## 本次目标
- 提升稳定性：降低进化循环中 `LLM ERROR 529 overloaded` 的重复失败概率。
- 提升任务执行准确率：收敛到可验证、低 blast radius 的修复路径。
- 提升跨 agent 协作质量：让 bridge/executor 协作链路更可观测、可追踪。

## 执行摘要
- 已运行一次 capability-evolver：`EVOLVE_STRATEGY=harden node index.js run`。
- 本轮由 selector 选择 `gene_gep_repair_from_errors`，触发信号为 recurring error（529 overloaded）。
- mutation 目标为 `gene:gene_gep_repair_from_errors`，风险等级 low，预期效果为提升成功率与降低重复操作成本。
- 本轮未检测到落地代码改动（无可提交的源码变更），输出以策略收敛和后续修复建议为主。

## 关键改动 / 建议
1. 退避重试策略（高优先级）
   - 在 evolver 的 LLM 调用链加入指数退避 + 抖动（jitter），并设置最大重试上限。
   - 对 529/overloaded 单独分类，避免与逻辑错误同路径处理。

2. 任务降级与切换策略
   - 当同类 overloaded 在短窗口内连续触发时，自动降级到更轻量模型/更短上下文。
   - 将复杂任务切为小任务，优先执行可验证的 repair-only 子步骤。

3. 跨 agent 协作可观测性
   - 为 bridge -> executor 的链路增加 run_id 贯穿日志，记录 spawn 成功/失败、执行时长、最终状态。
   - 固化最小协作状态机：`spawned -> running -> solidified|failed`，避免“只打印 spawn 指令但未真正执行”这类灰区。

4. 准确率护栏
   - 在 solidify 前新增一次“协议完整性检查”：5 个必需对象缺一即 fail-fast。
   - 对 validation 命令执行结果做结构化记录，避免“执行了但不可审计”。

## 风险
- 过强重试策略可能增加 token 消耗与时延。
- 自动降级模型可能降低单次修复质量，需要与稳定性收益平衡。
- 协作状态机改造涉及调度/日志接口，若无回归测试会引入新的边缘故障。

## 下一步
- N1: 先实现 529 overloaded 的指数退避与分类重试（最小改动）。
- N2: 增加 bridge/executor run_id 追踪与状态落盘，确保协作链路可审计。
- N3: 补一组最小回归测试：重试策略、状态机流转、solidify 前协议校验。
- N4: 在下一次循环验证三项指标：失败率、首次成功率、协作链路完成率。

## 证据摘录
- run_id: `run_1772564467523`
- selected_gene_id: `gene_gep_repair_from_errors`
- trigger_signals:
  - `recurring_error`
  - `recurring_errsig(5x): LLM ERROR 529 overloaded`
- mutation_risk_level: `low`
