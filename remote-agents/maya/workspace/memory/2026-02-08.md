# 2026-02-08

## 今日关键变更（可持续）

- Rain 要求暂停 `Crypto Watch (majors)`，已将该 cron 任务设为 `disabled`（未删除）。
- `Crypto 市场总结（09:00/16:00/22:00）` 的流程被多次优化并最终确定：
  - 仅在【市场动态（X）】调用 Grok；其他小节由主模型生成。
  - Grok 使用 `--thinking` + `--max-tokens 1024`。
  - X 叙事窗口改为“最近 6 小时”（不是 24h）。
  - 强约束为“仅 crypto 相关叙事”，排除非加密宏观/股市噪音。
  - 排版约束增强：标题独占行、分节固定、列表逐条换行。
- 修复了 `x-search` 脚本的 citation 解析崩溃（`sort_by(.title|tonumber)` 导致 null/非数字报错），改为安全排序与空值兜底。

## 同步与基础设施

- Rain 决定弃用 `claw-roam`（Git 同步）并切换到 Syncthing 双向同步。
- 已执行：
  - 移除 `~/.openclaw/workspace` 的 symlink，恢复为真实目录并保留 `.git` 历史。
  - 安装并启用 `syncthing@ubuntu.service`。
  - 同步目录设为 `~/.openclaw/workspace`（folder id: `openclaw-workspace`）。
  - 新增 `.stignore` 模板用于排除敏感/机器本地文件。
  - 停用 `skills/claw-roam`（移动到 `skills/_disabled/...`）。
- 与 Mac 已完成设备配对并验证双向同步成功（测试文件可互通）。
- 新增第二个同步目录：`~/.config/google-workspace-mcp`（folder id: `google-workspace-config`）。

## 邮箱/Google 方向决策

- Rain 明确不希望走邮箱密码方案（IMAP/SMTP），偏好 OAuth。
- 已停用旧 Maton Gmail 方案，安装 `google-workspace-mcp` 技能（支持 Gmail/Calendar/Drive/Docs/Sheets，Slides 以读取工具为主）。
- 当前 VPS 侧 `google-workspace-mcp` 调用存在握手/超时问题（server offline/timeout）；即使配置目录同步，仍需额外打通步骤，后续再处理。

## 使用偏好

- Rain 对长流程配置可接受，但要求：
  - 明确成本控制（尤其 Grok 调用）
  - 输出排版稳定
  - 方案优先“可持续运维”（如 Syncthing）
