# 2026-02-07

## Durable updates

- 用户确认了跨设备同步策略：保留 `main` 作为共享基线；设备分支命名为 `remote`（服务器）和 `local`（本地），并通过 `claw-roam` + `merge-from` 工作流同步。
- `claw-roam` 已升级并发布到 ClawHub：`claw-roam@1.0.2`（包含多分支同步相关改进）。
- 新建并上线交易日志能力（私用）：`trading-journal`。
  - 数据存储在 Git/workspace：`trading/insights.md`、`trading/rules.md`。
  - 记录策略为：LLM 自动归类（type/style/tags/one），并回显“仅当前新写入条目”+简短点评。
  - 用户偏好：记录以感性复盘为主，不做强量化字段。
- 提炼出交易三条核心规则并写入 `trading/rules.md`：
  1) 下单前 5 秒复核（反情绪/反消息）
  2) 止损只服从预设失效点（禁止随意止损）
  3) 极端行情用规则化方案，不靠熬夜硬盯
- 提醒系统改为 `reminder`（替代 planning 命名），并发布 `reminder@0.1.1` 到 ClawHub。
  - 事件数据与 skill 逻辑分离：数据在 `reminders/events.yml`，skill 在 `skills/reminder/`。
  - 默认提醒策略：Asia/Shanghai，提前 24h/1h/10m，发 Telegram。
- 已创建并启用具体提醒：
  - 2026-02-08 10:00（上海）“会”提醒（1h/10m 还有效）
  - 2026-02-07 14:00（上海）“寄快递”提醒（已在当天触发提醒）
- 建立加密盯盘能力 `crypto-watch`：
  - 仅监控 `BTC/ETH/SOL`。
  - 5 分钟轮询（用户确认保持 5m）。
  - 采用 15m、300 根 K 的结构化判断（突破/反弹/异常），并在触发时支持发送宽屏 K 线图。
  - 当前数据源优先 Binance API，若受限自动 fallback 到 OKX。
- 多媒体处理偏好已确认：
  - 语音转写仅作为输入，不默认回显转写文本。
  - 转写文本与图片不入 workspace，使用临时目录（`/tmp` 或 `~/.openclaw/media/inbound` 原始入站）。

## User preferences reaffirmed

- 沟通风格：直接、少废话、重可执行。
- 交易辅助：重“看图+结构”判断（道氏/量价/威科夫取向），不要均线主导。
- 先试运行，再迭代参数（盯盘阈值/频率/提醒策略）。

## Later updates

- 用户确认：crypto-watch 继续保持 **每 5 分钟**检查（不降到 10m/15m）。
- 用户已把模型切换到 `openai-codex/gpt-5.3-codex`，并确认当前会话已生效。
- 已向用户解释本机 OpenClaw 版本 `2026.2.6-3` 对应的 2026.2.6 主要更新（模型支持、Cron 稳定性/行为改动、安全加固等）。
