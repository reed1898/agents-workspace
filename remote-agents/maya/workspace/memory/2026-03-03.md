
## Durable updates (memory flush)
- Reed明确偏好：多 Agent 宪章更新流程由 Maya统一负责（改宪章→推 `agent-network-data`→必要时更新skill→下发执行与ACK校验）。
- 关键边界：`~/.openclaw/shared/agent-network-data` 是多 Agent 协调核心数据仓，不属于本地知识库（KB），不得按“清理知识库”处理。
- 宪章已作为统一规则源持续更新并推送：
  - 新增长耗时任务规则：预计>30秒/复杂任务优先下发 sub-engine/subagent，主会话仅编排与反馈，避免阻塞聊天。
  - 新增 Skill 研发规则：禁止硬编码绝对路径、配置驱动、兼容 Linux/macOS/Windows。
  - 新增汇报规则：所有汇报默认 Telegram Bot 私聊 Reed；且汇报需先写入各 agent 私有知识库并可追溯路径/ID。
- `reed-agent-network` skill 已升级到 0.2.2：`init` 可自动确保/同步 `AGENT_CONSTITUTION.md` 索引到 `MEMORY.md`（幂等）。
- `volcengine-stt` 已落地并验证可用：
  - 采用火山标准版接口（submit/query）可成功转写。
  - 配置优先使用 `skills.entries["volcengine-stt"].env.VOLC_*`。
- `knowledgebase-share` 修正为通用配置驱动（避免暴露用户仓库/路径）：
  - 发布到 `knowledgebase-share@0.3.2`。
  - 新增 `references/kb-config.json`，脚本按配置读取并支持 safer 模式（如 `--no-push`）。
- 已新建知识库存储仓库：`reed1898/agent-knowledge-layer`，用于多 Agent knowledge layer（private/shared 分层 + PR 合并）。
- OpenClaw 安全加固已执行关键项：Telegram DM 政策改为 allowlist、群命令加 allowFrom、`~/.openclaw/credentials` 权限收紧为 700。
- 群聊规则再次确认：`GROUP_RULES.md` 是群消息唯一生效源；`@all` 时所有 agent 必须回复，不能被旧的“他 bot 提及静默”规则覆盖。
- 用户要求“彻底解决，不要只修一个点”：处理多 Agent 群消息问题时要做规则源统一 + 冲突规则清理 + 回归验证，不接受临时补丁式答复。
- 用户确认将 Evolver 放入 heartbeat：每4小时跑一次 `--review`；若有文件/配置改动提案，先发 Telegram 审批（同意/拒绝）后再执行。
- Reed Agent OS 运行时间偏好已确认：每天 08:30 和 21:30（Asia/Shanghai）自动执行。
- 用户要求跨 agent 询问可直接走 Discord channel（而非仅依赖 agent-to-agent 会话通道）。
- X/Twitter 内容抓取默认工具再次确认：优先使用 `x-tweet-fetcher`（避免 x.com 网页反爬导致抓取失败），并要求同步到其他 agent 一致执行。
- Discord 头像/名字排障经验：改 App 的 General Information 不一定生效到聊天显示；应在 Developer Portal 的 Bot 页修改 Username/Avatar，并检查服务器内是否有 server-level 昵称/头像覆盖。
