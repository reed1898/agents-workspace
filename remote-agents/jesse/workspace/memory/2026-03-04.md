# 2026-03-04

- Reed 明确要求：查询“我的持仓情况”时，必须基于数据库实时数据，不允许主观猜测或瞎说。
- 持仓数据源：`trading_notes` MySQL（连接信息由 Reed 提供），重点表包括 `positions`、`closed_positions`、`trade_accounts`。
- 默认做法：先查库再总结；若币种口径混合（CNY/USD/USDT）需明确标注“粗略汇总”或先做统一汇率换算。
- Reed 于 2026-03-04 20:09 提供了 `trading_notes` 的 DATABASE_URL；后按 Reed 要求改为带前缀命名，落地为本机用户级环境变量：`TRADING_NOTES_DATABASE_URL` + `TRADING_NOTES_MYSQL_*`，并清理通用 `DATABASE_URL`/`MYSQL_*` 以避免多库冲突。
