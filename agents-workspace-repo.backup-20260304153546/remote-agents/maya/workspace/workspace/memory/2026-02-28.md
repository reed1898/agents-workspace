# 2026-02-28

- Reed 确认并要求记忆：Telegram 多 Agent 群聊回复机制（含角色定义与协作规则）。
- 角色定义：Reed（最高优先级）、小洪（观察/学习/协同）、Maya（总控协调总结）、Jesse（交易风控/信息/策略）、Linus（与 Reed 共建 AI 产品）。
- Telegram 群聊规则已收敛到 `GROUP_RULES.md`（唯一生效源）；此前分散规则作废。
- Reed 再次强调并要求写入 memory：核心配置改动审批规则——任何 agent 修改 OpenClaw 核心配置（如 `~/.openclaw/openclaw.json` / gateway 相关）前，必须先向 Reed 发 Telegram 变更申请，说明“改什么/为什么/风险/回滚方案”，并等待明确同意；未获同意禁止改动。（2026-02-25）
- Reed 再次强调并要求写入 memory：审批交互样式规则——核心配置改动默认使用“审批卡片 + 可点击按钮”在 Telegram 发起（`✅同意执行` / `❌拒绝执行`）；卡片必须包含改动项、原因、风险、回滚方案；必要时要求二次确认词（如 `CONFIRM`）后执行。（2026-02-25）
